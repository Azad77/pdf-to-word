Django==5.1.5
gunicorn  # Required for running Django on Render
psutil  # Optional but useful for monitoring
pdfplumber  # Required for PDF processing
pdf2docx  # Required for PDF to Word conversion
PyMuPDF  # Alternative PDF processing library
pypdfium2  # PDF rendering and manipulation
python-docx  # Working with .docx files
requests  # If your app makes HTTP requests
whitenoise  # Helps serve static files in production
dj-database-url  # If using PostgreSQL
psycopg2-binary  # Required for PostgreSQL
